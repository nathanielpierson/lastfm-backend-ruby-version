<%= form_with(model: get_top_album) do |form| %>
  <% if get_top_album.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(get_top_album.errors.count, "error") %> prohibited this get_top_album from being saved:</h2>

      <ul>
        <% get_top_album.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
